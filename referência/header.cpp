#include <bits/stdc++.h>
using namespace std;

#define pb push_back
#define eb emplace_back
#define st first
#define nd second
#define all(x) x.begin(), x.end()
#define mset(x, y) memset(&x, (y), sizeof(x))

typedef long long ll;
typedef unsigned long long ull;
typedef long double ld;
typedef pair<int, int> ii;

const int MOD=1e9+7;//primo
const int MOD2=1e9+9;//primo

const int inf=0x3f3f3f3f;
const ll infll=0x3f3f3f3f3f3f3f3f;
const double eps=1e-9;
const double PI = 2*acos(0.0);


__builtin_ffs(int)//Retorna 1 + posição do bit 1 menos significativo. zero para 0
__builtin_clz(int)//Retorna o número de zeros na frente do bit 1 mais significativo
__builtin_ctz(int)//Retorna o número de zeros atrás do bit 1 menos significativo
__builtin_popcount(int)//Soma dos bits
__builtin_parity(int)//Soma dos bits módulo 2

int main(){}
