#include <bits/stdc++.h>
using namespace std;

int main()
{
  int n;
  map<string, int> m;
  while(true){
    scanf("%d", n);
    if(!n)break;


    while(n--){
      string a;
      cin >> a >> a;
      m[a]+=1;
    }
  }
  for(auto x:)
}
